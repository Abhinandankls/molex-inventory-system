
# Molex Inventory System (Enterprise Edition)

## 🚀 How to Run in VS Code
1. Open Terminal (`Ctrl` + `~`).
2. Type **`node server.js`** and press Enter.

*Note: The system will automatically install dependencies, build the app, and generate the necessary `.bat` files for future use.*

## ✨ Features
### 1. Photo Upload 📸
- Go to **Inventory** Tab.
- Click **Add Part** or the **Edit (Pencil)** icon.
- Click **Select Photo** to upload an image of the spare part.

### 2. Consumption Analytics 📊
**For Supervisors:**
- Go to the **Consumption** tab.
- View "Top Consumers" graphs and Leaderboard.
- Use **"Reset Monthly Counters"** to clear the current period's stats (History is saved in Logs).

**For Operators:**
- Click **"My Stats"** (Top Right) to see personal usage graphs.

### 3. Security
- **Admin PIN:** Accessing Supervisor mode requires a PIN (Default: `1234`).
